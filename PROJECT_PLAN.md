# 6Gang 项目计划

---

## 项目到底要做什么

**1. 群体画像**：把 70+ 篇射击游戏玩家访谈笔录，变成一个可对话的虚拟玩家群。

- 用冰山模型给每段玩家原话打标签（M5 行为 → M4 感受 → M3 认知 → M2 期待 → M1 动机）
- 聚类形成 5-8 种典型玩家画像，每种画像包含：标签组合 + 动机因果链 + 代表性原声证据
- 做对话界面：用研人员选标签 → 匹配画像 → 像跟真人深访一样提问 → AI 用画像人设回答 → 可溯源到原始语料

**2. KOL 分身**：基于 B 站 UP 主的公开内容，构建个体级数字孪生。

- **数据来源**：两位 KOL 的 B 站全量内容
  - 冷面叶星星IKGN（`space.bilibili.com/518045432`）——硬核测评型
  - 鬼王陆行（`space.bilibili.com/1628647`）——硬核测评型
- **做什么**：
  - 抓取两位 UP 主的全量视频/动态内容 → 视频转文字 → 文本清洗
  - 提取个人风格特征：口头禅、高频词、句式习惯、语气与表达节奏
  - 构建底层人设：游戏审美标准、评价体系与权重、内容创作偏好、受众认知、商业合作态度
  - 形成可对话的 AI 分身——用研经理可以和这个分身进行 1v1 深度访谈，问它对某款新游戏/新玩法的看法，分身以该 UP 主的风格和评价逻辑回答
- **与群体画像的区别**：群体画像模拟"某一类人"，KOL 分身模拟"某一个具体的人"。前者追求类型代表性，后者追求个人拟合度——要让熟悉该 UP 主的人觉得"这话就是他说的"
- **当前状态**：已有 84 道 KOL 测试题集（一致性 + 立项判断 + 推广合作 + 设计反馈），但没有已抓取的内容数据。本期暂缓，下期可直接用测试题集验收

---



## 可用材料


| 材料                | 在哪                                               | 干嘛用                 |
| ----------------- | ------------------------------------------------ | ------------------- |
| 70+ 篇访谈笔录 (.docx) | `虚拟用户-笔录 for 元培/`                                | AI 打标的原料            |
| 枪战用户分析框架 (.xlsx)  | `射击品类用研知识输入-枪战用户分析框架.xlsx`                       | 标签体系来源              |
| 心流体验量表 (28 题)     | 同上，附录 Sheet                                      | 甜区维度标注参考            |
| 冰山模型              | 项目介绍 Slide 22-23                                 | 标注方法论               |
| **群体画像测试题集**      | `AI模拟用户画像_测试题集_射击类用户(2).xlsx`                    | **AI 输出质量基准，142 题** |
| **KOL 测试题集**      | `KOL数字孪生_测试题集_硬核测评KOL(2).xlsx`                   | KOL 方向参考，下期直接用      |
| 项目脚手架             | `https://github.com/julia-lars/tengxun_yp_6Gang` | 队伍的所有内容都整理在这个仓库里    |




### 测试题集结构

**群体画像**（142 题，5 个 Sheet）：


| Sheet        | 题数  | 验证什么                                                |
| ------------ | --- | --------------------------------------------------- |
| 一致性测试        | 35  | 同一画像回答不自相矛盾、不同画像间可区分。覆盖维度：地域/能力/动机/风格/平台/社交/付费/内容消费 |
| 假设性测试-游戏立项   | 24  | 面对"我们打算做一款XX游戏"时，给出的品类/题材/美术/付费偏好是否符合画像             |
| 假设性测试-玩法设计   | 34  | 面对具体设计方案（TTK/地图/技能/武器/载具/社交系统）时的偏好判断                |
| 假设性测试-运营与商业化 | 24  | 面对付费设计/战令/抽奖/活动/赛季重置/召回时的态度和行为                      |
| 假设性测试-市场营销   | 25  | 面对不同卖点/KOL推荐/IP联动/明星代言/赛事/渠道时的反应                    |


---



## 各职能任务



### 产品经理 × 1（类恺平）

负责：定义产品价值、使用场景、功能方案，把控 MVP 范围，确保交付可演示。

**任务 1：用户流程与场景定义**

用研经理打开产品后要完成什么任务？每一步怎么走？把完整路径画出来。

- 梳理两类核心用户（用研经理、游戏策划/运营）各自的典型使用场景
- 画出完整用户流程图：从打开产品 → 选择标签 → 预览画像 → 进入访谈室 → 提问对话 → 查看溯源 → 导出洞察
- 标注每一步中哪些是 AI 自动完成的（画像匹配、回答生成）、哪些需要用户手动操作（选标签、输入问题）
- 明确异常路径：选完标签没有匹配画像怎么办？对话中 AI 答非所问怎么办？
- 产物：用户流程图 + 场景说明文档

**任务 2：标签体系产品化设计**

用研专家的枪战分析框架是专业文档，需要翻译成产品里能用的交互方案。

- 基于框架的 5 个维度（诉求/资产/能力/风格/甜区），设计标签选择器的产品方案
- 定义每个维度的可选标签值域（如"能力"维度：新手/进阶/高手/职业级）
- 明确标签之间的互斥规则（如"纯PVE玩家"和"竞技核心玩家"不能同时选）
- 定义标签组合规则（哪些维度是必选、哪些可选、默认值是什么）
- 设计实时反馈——选标签过程中随时显示"当前标签组合下匹配到 N 个画像"
- 产物：标签体系产品方案文档

**任务 3：竞品简要调研**

看看市场上类似产品怎么做，提炼可借鉴的交互模式，不用长篇大论。

- 体验 Character.AI（角色对话）、Synthetic Users（AI 模拟用户调研）、Delve AI 等 2-3 款产品
- 重点关注：画像/角色的创建流程、对话界面的交互设计、上下文记忆和一致性如何体现
- 输出：可借鉴的交互模式 + 要避开的坑 + 对本项目的建议
- 产物：竞品调研一页纸

**任务 4：演示脚本**

结课汇报就是一场 Live Demo，需要提前设计好每一步。

- 从测试题集 5 个 Sheet 各挑 2-3 道最有代表性的题
- 设计演示路径：选什么标签 → 生成什么画像 → 依次问什么问题 → 展示溯源 → 换一组标签对比不同画像的回答差异
- 准备 Plan B：如果某个环节出 bug，怎么跳过继续演示
- 准备结课汇报 PPT（产品定位、核心功能、演示环节说明）
- 产物：演示脚本 + 结课汇报 PPT

---



### 技术负责人 × 1（朱丽烨）

负责：技术方案定稿、架构把控、环境与 CI/CD、难点攻关、代码质量。

**任务 1：技术方案定稿**

把所有技术决策确定下来，一页纸讲清楚。

- LLM 模型选择：DeepSeek vs GLM vs MiniMax，明确主力模型和备选，写清楚选择理由
- RAG 架构：用 pgvector 做向量检索（减少技术栈复杂度），检索策略（top K、相似度阈值、重排序方案）
- 聚类方法：HDBSCAN 参数设定（min_cluster_size 等），配合 UMAP 降维可视化验证聚类效果
- Embedding 方案：推荐 bge-large-zh（中文效果好、1024 维、开源），备选 text-embedding-3-small
- 产物：技术选型记录（一页纸）+ 系统架构图

**任务 2：数据库与 API 设计 Review**

和后端、AI 开发一起确认设计。

- 审核 `source_segments`、`personas`、`chat_sessions` 三张表的字段设计，确保能存下标注结果和对话记录
- 审核 pgvector 索引方案（HNSW vs IVFFlat，参数如何设）
- 审核 API 路由设计：RESTful 风格是否一致、SSE 接口的错误处理
- 和 AI 开发确认数据 Pipeline：docx → 清洗 → 打标 → embedding → 聚类 → 入库，每步的数据格式约定
- 产物：设计 Review 记录

**任务 3：环境与 CI/CD**

脚手架已跑通，确保能一直稳定、最后能部署上线演示。

- 当前环境：Bun + PostgreSQL 本地，86 测试全绿
- 验证 GitHub Actions CI 流水线（`ci.yml` 模板已存在，推送代码后确认 lint + typecheck + test + build 全绿）
- 确认 CD 流水线就绪：构建 Docker 镜像 → 推腾讯云 TCR → CVM 部署
- 交付前一天完成生产环境部署，确保线上可访问
- 产物：CI/CD 就绪 + 生产环境 URL

**任务 4：技术难点攻关**

三个最可能卡住的地方，提前验证方案。

- 角色一致性：多轮对话中如何维持人设不崩？方案——System Prompt 中硬性约束 + 对话历史摘要压缩 + 每轮注入画像核心标签
- SSE 流式对话：Hono 框架的 SSE 实现方案，前端 EventSource 对接，断线重连
- RAG 检索延迟：pgvector HNSW 索引参数调优，目标检索 < 500ms
- 产物：技术难点 PoC 验证

**任务 5：Code Review 机制**

保证多人协作时代码质量。

- 分支规范：从 main 拉 feature 分支 → 开发 → 提 PR → TL 审核 → 合并
- PR 必须通过 CI（lint + typecheck + test）才能合并
- 每次 CR 关注：类型安全（有没有 any）、错误处理（有没有吞异常）、文件组织（放对位置）
- 产物：Git 工作流 + CR Checklist

**任务 6：技术总结**

结课汇报的技术部分。

- 架构设计亮点（四层架构、数据 Pipeline、RAG 方案）
- 关键决策和取舍（为什么选 pgvector 而不是独立向量库、为什么用 HDBSCAN 而不是 K-Means）
- 可复用资产（脚手架、CI/CD 模板、LLM SDK 封装）
- 产物：技术总结（用于结课汇报 PPT 技术部分）

---



### AI 开发 × 3（崔续衡 闫瑾 林钰坤）

负责：数据 Pipeline、LLM 封装、Prompt 工程、画像生成引擎、对话引擎、评测脚本。

**任务 1：数据提取与清洗**

把 70+ 篇 docx 笔录变成干净的可处理文本。

- 写 Python/Bun 脚本：用 `python-docx` 批量读取所有 docx 文件，提取纯文本
- 按段落分片（以访谈中自然的话题切换为界），去除空行、主持人提问、重复寒暄
- 中英文分别处理（海外搜打撤研究有 16 篇英文笔录），英文翻译成中文。
- 输出统一格式：`{ "source_file": "xxx.docx", "segment_id": 1, "text": "..." }`
- 产物：`cleaned_corpus.jsonl`（预计数千条片段）

**任务 2：打标 Schema 设计**（与用研专家协作）

把标注规范翻译成 LLM 能理解和输出的结构化 JSON Schema。

- 基于用研专家的标注规范，设计完整的 JSON Schema
- 必须覆盖测试题集列出的所有区分维度：
  - 冰山五层：M5 行为、M4 感受、M3 认知、M2 期待、M1 动机
  - 枪战框架：诉求（一般游戏诉求 + 枪战竞技诉求 + 风格偏好）、资产（时间/能力/精力/情绪/金钱）、能力（场景-技巧-认知三级）、甜区体验
  - 客观属性：地域/城市等级、游戏经验平台分布、PVE/PVP 偏好
- 每个字段定义值域（enum 或自由文本）、是否必填、置信度要求
- 用几个黄金案例测试 Schema 是否能完整表达标注内容
- 产物：`annotation_schema.json`

**任务 3：LLM SDK 封装**

统一 LLM 调用入口，所有 AI 功能模块都调这个，不直接调 API。

- 封装 `apps/api/src/lib/llm.ts`：
  - `chat(messages, options)` — 普通对话，返回完整回复
  - `chatStream(messages, options)` — 流式对话，返回 AsyncIterator
  - `embed(text)` — 文本向量化
- 支持模型切换：通过环境变量或参数指定 DeepSeek/GLM/MiniMax
- 内置：3 次重试（指数退避）、30s 超时、Token 用量统计、请求日志
- 统一的错误处理：API 不可用时返回明确的错误类型（不是笼统的 500）
- 产物：`apps/api/src/lib/llm.ts`

**任务 4：AI 打标 Pipeline**

这是整个项目最关键的环节——打标质量直接决定后续画像和对话的质量。

- 写打标 System Prompt：告诉 LLM "你是用户研究员，请按以下 JSON Schema 分析这段玩家访谈"
- Few-shot 示例：将 30 条黄金案例按格式嵌入 Prompt
- 批量打标流程：读取 `cleaned_corpus.jsonl` → 逐条调 LLM 打标 → 解析 JSON 输出 → 校验 Schema 合法性 → 入库
- 质量控制：LLM 输出的 confidence 低于 0.6 的片段自动标记为"待人工复核"
- 错误处理：LLM 返回非 JSON → 自动重试 1 次 → 仍失败则标记为"打标失败"
- 进度追踪：输出打标进度（已处理 X/总数），预估剩余时间
- 产物：打标脚本 + `annotated_corpus.jsonl`

**任务 5：Embedding 与向量存储**

把打标后的文本变成可检索的向量。

- 选型：推荐 `bge-large-zh-v1.5`（1024 维，中文 SOTA，HuggingFace 开源）
- 写 embedding 脚本：读取每条的 `original_text` → 调 embedding API → 存向量
- 在 PostgreSQL 中启用 pgvector 扩展，建 `source_segments` 表（含 `embedding vector(1024)` 字段）
- 建 HNSW 索引（ `USING hnsw (embedding vector_cosine_ops)`），配置 `m=16, ef_construction=200`
- Chunking 策略：长文本按 500 字切片，相邻切片 overlap 100 字
- 产物：`apps/api/src/db/schema.ts`（新增表 + embedding 索引）+ embedding 脚本

**任务 6：画像生成引擎**

把一堆打了标的文本片段变成可被选择的用户画像。

- 特征构建：对每个片段，将冰山标注和框架标注合并为特征向量（文本 embedding + 标签 one-hot 编码拼接）
- 聚类：HDBSCAN（`min_cluster_size=5` 起步，根据实际数据调整），配合 UMAP 降维到 2D 可视化验证
- 每个 cluster 提取：
  - 标签组合：统计该 cluster 内高频标签值 → 形成画像标签
  - 动机因果链：从 M1 到 M5 的典型路径（"因为什么动机 → 产生什么期待 → 形成什么认知 → 什么感受 → 什么行为"）
  - 代表性原声：选 3-5 条最靠近 cluster 中心的原文片段作为"这个画像的典型发言"
- 画像命名：根据标签组合自动生成描述性名称（如"竞技驱动·高能力·PC 端·独狼型"）
- **验收**：生成的 5-8 个画像在测试题集一致性测试中能否表现出区分度
- 产物：`apps/api/src/lib/persona-engine.ts` + `/api/personas`（含画像列表和详情接口）

**任务 7：对话引擎**

让画像"活过来"——用户提问，画像用对应人设回答。

- Persona Card → System Prompt：将画像定义翻译成 LLM 的角色设定
  - 模板结构：你是谁（画像名称+描述）+ 你的核心动机（M1-M2）+ 你的认知框架（M3）+ 你的行为模式（M5）+ 说话风格（基于代表性原声）+ 规则约束
- RAG 检索管道：用户问题 → 向量化 → pgvector 相似搜索 → 取 top 3 最相关原文片段 → 注入 System Prompt 作为"你知道这些事"
- SSE 流式接口：`POST /api/chat` 返回 `text/event-stream`，逐 token 推送
- 对话记忆：维护滑动窗口（最近 6 轮），超出部分做摘要压缩；每次请求带画像 ID + 会话 ID
- 知识边界控制：用户问超出射击品类/游戏领域的问题 → AI 应识别并回答"这个话题我不太了解，你可以问我关于射击游戏的事"
- **验收**：用测试题集 142 道题逐类检验——一致性不崩（同画像不矛盾）、假设性合理（不同画像给出不同的、符合其人设的回答）
- 产物：`apps/api/src/lib/chat-engine.ts` + `/api/chat`（SSE 流式）

**任务 8：Prompt 迭代优化**（与用研专家协作）

Prompt 不是一次写对的，要根据评测反馈持续改。

- 从用研专家的打标审核报告中提取问题模式（如"LLM 总是把'M3 认知'标成'M4 感受'"）
- 从测试的评测报告中提取对话问题（如"竞技型画像在被问到社交话题时回答太温和，不像竞技玩家"）
- 改写 Prompt：调整指令措辞、增加反例、修改 Few-shot 示例选取
- 每次改 Prompt 后重新跑评测脚本，对比分数变化
- 记录每次迭代的改动点和效果
- 产物：Prompt 版本记录 + 优化日志

**任务 9：自动化评测脚本**

把测试题集的 142 道题变成能一键跑的评测流程。

- 解析两个 xlsx 文件 → 转为结构化的测试用例 JSON
- 每条测试用例：`{ "sheet": "一致性测试", "dimension": "游戏水平/能力", "question": "...", "expected_dimension": "..." }`
- 评测流程：选对应画像 → 发问题 → 收回答 → LLM-as-judge 打分（1-5 分，评估"这个回答是否与画像设定一致/是否合理"）
- 汇总报告：按 Sheet、按维度分组的平均分、最低分、最高分；列出低分条目供人工复核
- 产物：`apps/api/src/lib/eval.ts` + 评测报告模板

---



### 产品开发 × 2（崔续衡 张江宇）

负责：一人后端（数据库 + API + AI 对接），一人前端（五个页面）。

#### 后端开发（1 人）

**任务 1：数据库建表与 Migration**

在当前 Drizzle Schema 基础上新增三张业务表。

- `source_segments` 表：
  - `id` serial PK
  - `source_file` text — 来源文件名（如"座谈会笔录-G1.docx"）
  - `segment_index` integer — 在原文中的段落序号
  - `original_text` text NOT NULL — 原始文本
  - `cleaned_text` text — 清洗后文本（去口语填充词）
  - `annotation` jsonb — 冰山五层 + 框架标注 JSON
  - `embedding` vector(1024) — bge-large-zh 向量
  - `persona_ids` integer[] — 关联的画像 ID
  - `created_at` timestamptz DEFAULT now()
- `personas` 表：
  - `id` serial PK
  - `name` text NOT NULL — 画像名称
  - `description` text — 描述
  - `tag_spec` jsonb NOT NULL — 标签组合定义
  - `motivation_chain` jsonb — M1→M5 动机因果链
  - `evidence_ids` integer[] — 代表性原声片段 ID
  - `cluster_id` text — HDBSCAN 聚类 ID
  - `sample_count` integer — 该画像基于多少样本
  - `created_at` timestamptz DEFAULT now()
- `chat_sessions` 表：
  - `id` serial PK
  - `persona_id` integer REFERENCES personas(id)
  - `messages` jsonb NOT NULL DEFAULT '[]' — [{role, content, evidence_ids, timestamp}]
  - `created_at` timestamptz DEFAULT now()
  - `updated_at` timestamptz DEFAULT now()
- 运行 `bun run db:generate` → 生成 migration SQL → `bun run db:migrate` → 验证表结构
- 产物：Drizzle Schema 更新 + Migration 文件

**任务 2：共享类型定义**

前后端共享的 Zod Schema，写在 `packages/shared/src/types.ts`。

- `Persona`：id, name, description, tagSpec, motivationChain, evidenceIds, sampleCount
- `PersonaDetail`：Persona + evidenceTexts（展开的原声片段列表）
- `TagDimension`：dimension（维度名）, values（可选标签值数组）, selected（已选值）
- `ChatMessage`：role（'user' | 'assistant'）, content, evidenceIds?, timestamp
- `ChatRequest`：personaId, sessionId?, message
- `ChatResponse`：content, evidenceIds（关联的原文片段 ID）, sessionId
- `AnnotationResult`：segmentId, annotation（冰山五层 + framework）, confidence
- `Evidence`：id, sourceFile, originalText, annotation, similarity（检索相似度）
- 产物：`packages/shared/src/types.ts` 新增类型

**任务 3：API 实现**

五个接口，按 Hono 现有路由模式（参考 `apps/api/src/routes/courses.ts`）实现。

- `GET /api/tags` — 返回可用标签维度列表和每个维度的值域。从用研专家的标签体系产品方案中提取，硬编码或从配置表读取
- `GET /api/personas` — 画像列表。支持 `?tags=竞技驱动,高能力,PC端` 查询参数，后端按标签匹配度排序返回。每个画像返回摘要信息
- `GET /api/personas/:id` — 画像详情。展开完整标签、动机因果链、代表性原声片段（含原文和标注），方便用研经理深入了解这个画像
- `POST /api/chat` — 核心对话接口。接受 `{ personaId, sessionId?, message }`，调用 AI 对话引擎，返回 SSE 流式响应（`text/event-stream`）。每个 `data` 事件包含 `{ content_chunk, evidence_ids }`
- `GET /api/chat/sessions/:id` — 返回某个会话的完整对话历史
- 所有接口加 Zod 请求校验（参考 `demo-sandbox.ts` 里的 `zValidator` 用法）
- 产物：`apps/api/src/routes/personas.ts` + `apps/api/src/routes/chat.ts`

**任务 4：对接 AI 引擎**

把后端 API 和 AI 开发的画像引擎、对话引擎连起来。

- 对接画像生成引擎：调用 `persona-engine.ts` 的聚类和画像提取函数，将结果写入 `personas` 表
- 对接对话引擎：`POST /api/chat` 中调用 `chat-engine.ts` 的流式对话函数，处理 SSE 事件转发
- 错误降级：AI 引擎不可用时，返回预设的兜底回复（"模拟用户暂时无法响应，请稍后重试"），而非 500
- 产物：AI 对接层代码



#### 前端开发（1 人）

**任务 1：标签选择器页面**

用研经理进来第一件事——选标签定义目标用户。

- 布局：左侧标签维度列表（诉求/资产/能力/风格/甜区 五个分类），右侧已选标签摘要 + 匹配画像数量
- 交互：点击维度展开标签选项（芯片/Chip 样式），选中高亮，再次点击取消。互斥标签自动灰化（如选了"纯PVE"后"竞技核心"变灰）
- 实时反馈：顶部显示"当前标签组合匹配到 N 个画像"，N=0 时提示放宽条件
- 底部按钮："生成模拟用户"——点击后调 `/api/personas?tags=xxx` 跳转到画像预览页
- 参考：现有 shadcn/ui 组件（Badge 做标签芯片、Card 做维度卡片、Button 做操作按钮）
- 产物：标签选择器页面组件

**任务 2：画像详情页**

展示某个画像的完整信息，让用研经理了解"这是个什么样的玩家"。

- 页面结构：画像名称（大字标题）+ 一句话描述 + 标签列表（Badge 展示）+ 动机因果链（M1→M5 流程图或列表）+ 代表性原声（引用块样式，标注来源文件）
- 数据来源：`GET /api/personas/:id`
- 操作入口："开始对话"按钮 → 进入虚拟访谈室，带入该画像
- 产物：画像详情页组件

**任务 3：虚拟访谈室（对话界面）**

核心交互——和 AI 模拟玩家对话。

- 布局：左侧对话区（占主体），右侧画像信息卡（画像名称 + 标签摘要）
- 对话区：聊天气泡布局——用户消息靠右（蓝色），AI 消息靠左（灰色），头像用画像名称首字
- SSE 流式渲染：AI 回复时逐字显示（打字机效果），光标闪烁，生成完成后光标消失
- 输入区：底部固定，文本输入框 + 发送按钮，支持 Enter 发送
- 对话顶部：画像名称 + "模拟用户"标签 + 溯源提示"点击 AI 回答可查看依据"
- 产物：虚拟访谈室页面组件

**任务 4：证据溯源面板**

用研经理看到 AI 的回答后，想知道"你为什么这么说"——点一下就能看到依据。

- 交互：点击任意一条 AI 回答气泡 → 右侧滑出溯源面板（Sheet/Drawer 组件）
- 溯源内容：引用了几条原始语料片段，每条显示——原文（高亮关键句）、冰山标签路径（如"M1 动机: 归属感 → M3 认知: 独自玩=负面体验"）、来源文件名、与问题的相似度分数
- 技术实现：AI 回答的 `evidence_ids` 关联到 `source_segments` 表，前端调 `GET /api/personas/:id` 时一并获取
- 产物：溯源面板组件

**任务 5：路由与页面骨架**

把现有教程前端（课程列表、章节阅读器）替换为项目页面。

- React Router 路由配置（参考 `apps/web/src/main.tsx`）：
  - `/` → 首页（项目简介 + 两个入口：群体画像 / KOL 分身）
  - `/personas` → 标签选择器
  - `/personas/:id` → 画像详情
  - `/personas/:id/chat` → 虚拟访谈室
- 复用现有三栏布局（`root.tsx`），去掉教程内容，保留顶栏 + 侧栏导航
- 替换品牌视觉：Tailwind `@theme` 从元培学院风改为 MUR 暗色主题
- 产物：路由配置 + 页面骨架

---



### 交互设计 × 1（闫瑾）

负责：所有页面的交互方案和视觉风格，确保产品好用、好看。

**任务 1：标签选择器交互设计**

- 五个维度的分类展示方式（Tab 切换 vs 手风琴展开 vs 并列卡片）——哪个对用研人员最直观
- 标签选择/取消的视觉反馈（选中态高亮、hover 态、禁用态灰化）
- 互斥规则的呈现方式：是灰掉不可选的标签 + 提示原因，还是选了之后弹冲突提示
- 实时画像匹配数变化的过渡动效
- 产物：标签选择器交互稿 + 高保真 Figma 设计

**任务 2：虚拟访谈室交互设计**

- 对话区布局：气泡样式、间距、头像位置、时间戳是否显示
- 流式打字效果：逐字速度、光标样式、生成完成后的视觉提示
- 模拟用户信息卡：展示画像名称 + 核心标签 + "关于这个用户" 简短描述
- 溯源入口：每条 AI 回答旁的溯源图标，点击后的面板滑出方向和动画
- 对话状态指示：AI 生成中 vs 等待用户输入 vs 连接异常
- 产物：虚拟访谈室交互稿 + 高保真 Figma 设计

**任务 3：视觉风格定义**

- 产物：视觉规范文档 + styles.css 更新

---



### 测试 × 2（张江宇 林钰坤）

负责：已有 142 道现成题，不需要从零设计。重点是执行评测 + 自动化。

**任务 1：测试策略与题库整理**

- 定义测试层次和范围：
  - 单元测试（Vitest）：后端工具函数、数据校验逻辑 → 沿用现有 86 测试，新增业务的测试
  - 集成测试（pglite）：API 路由、数据库读写 → 参考现有 `courses.test.ts` 模式
  - E2E 测试：标签选择 → 画像生成 → 对话 → 溯源 完整流程
  - AI 评测：用测试题集 142 题对每种画像和场景做系统性评测
- 将 142 道题按画像类型和测试维度分类编号，方便追踪哪些已测、哪些未测
- 产物：测试策略文档 + 题库分类表

**任务 2：自动化评测脚本**

- 把 142 道题转为结构化 JSON 测试用例文件
- 写评测执行脚本：遍历用例 → 调对话 API 获取 AI 回答 → 保存回答
- 对接 AI 开发的 LLM-as-judge 评分模块，自动给每个回答打分
- 生成评测报告：按 Sheet、按维度汇总平均分，标出低分项
- 产物：评测脚本 + 评测报告

**任务 3：一致性评测**

用测试题集 Sheet 1 的 35 道一致性测试题逐画像验证。

- 对每种画像，用同一套题分别提问
- 检查同画像内部：不同题的回答之间是否有矛盾？（如一道题说"我喜欢刚枪"，另一道说"我一般苟着打"）
- 检查不同画像之间：同一道题，两个画像的回答是否有明显且合理的差异？（如竞技型说"输了继续排"，休闲型说"输了换游戏"）
- 多轮对话测试：选一种画像，连续对话 10 轮以上，检查中途是否崩人设
- 产物：一致性评测报告

**任务 4：假设性评测**

用测试题集 Sheet 2-5 的四组假设性测试题验证。

- 游戏立项（24 题）：AI 面对新游戏提案的反应是否符合画像的品类/题材/付费偏好
- 玩法设计（34 题）：AI 对具体设计方案的偏好判断是否符合画像的能力水平和风格
- 运营商业化（24 题）：AI 的付费意愿、活动参与度是否符合画像的资产约束和消费观念
- 市场营销（25 题）：AI 的信息渠道偏好、KOL 信任度、IP 联动兴趣是否符合画像特征
- 产物：假设性评测报告

**任务 5：盲测**

最终验收——AI 能不能以假乱真。

- 从测试题集中选 20 道代表性题目
- 用每种画像生成 AI 回答（5-8 套回答）
- 从原始笔录中提取对应类型真实用户的原文回答作为对照
- 将 AI 回答和真人回答混合，去掉标记，交给用研专家（或其他评委）盲评
- 评分标准：评委判断"这是真人还是 AI"，正确率越低说明 AI 拟合度越高
- 目标：评委区分准确率 ≤ 50%（即和瞎猜差不多）
- 产物：盲测报告

**任务 6：功能测试**

- 端到端走通完整用户流程：打开页面 → 选标签 → 查看画像 → 进入对话 → 提问 → 查看溯源
- 异常流测试：未选标签直接生成、选互斥标签、对话中刷新页面、网络断开后恢复
- 浏览器兼容：Chrome/Edge/Safari 最新版
- 产物：功能测试报告

**任务 7：性能测试**

- 画像生成速度：全量标签组合匹配画像的耗时
- 前端首屏加载时间
- 产物：性能测试报告

---



### 用研专家 × 2（类恺平 朱丽烨）

负责：领域知识底座——写标注规范、标注黄金案例、审核 AI 产出质量、校验画像准确性、准备盲测对照材料。

**任务 1：写标注规范**

冰山模型 + 枪战框架是理论，需要翻译成可执行的标注操作手册。

- 阅读冰山模型（Slide 22-23）和枪战框架 Excel 的全部 5 个 Sheet
- 针对冰山五层（M1 动机 → M2 期待 → M3 认知 → M4 感受 → M5 行为），每层写明：
  - 定义：这一层是什么（用一句话说清楚）
  - 标注方法：从用户原话的哪些词/句可以推断这一层
  - 值域：这一层有哪些可能的取值（如 M1 动机可能取"竞技证明/社交归属/放松逃避/探索收集/角色沉浸"等）
  - 正面示例：从笔录中找 2-3 条该层的典型原文 + 标注结果
  - 反面示例：容易标错的 case + 为什么错
  - 边界 case：介于两层之间怎么办（如"我想赢"是 M1 动机还是 M2 期待？）
- 针对枪战框架四维度（诉求/资产/能力/甜区），同样写清楚标注细则
- **关键约束**：标注值域必须覆盖测试题集 142 题中出现的所有区分维度（地域/能力水平/动机类型/风格偏好/平台偏好/社交方式/付费观念/内容消费习惯），确保标注结果能让不同画像在这些维度上表现出差异
- 产物：`标注规范.md`

**任务 2：标注黄金案例 30 条**

AI 打标需要参考标准——30 条人工精细标注的案例作为 Few-shot 示例。

- 从 70+ 篇笔录中挑选 30 条典型用户原话，挑选原则：
  - 覆盖不同类型的玩家（竞技核心/休闲社交/新手/硬核拟真/PVE 爱好者 等）
  - 覆盖不同的表达方式（直接陈述/情绪宣泄/对比评价/假设推演）
  - 覆盖中英文（海外笔录也要有几条）
- 对每条原话进行完整冰山五层 + 框架四维度标注
- 标注时标注者记下自己的推断逻辑（"我从'赢了说我躺输了说我菜'推断出 M2 期待是'队友应该配合而非评判'"）
- 这 30 条会直接嵌入 AI 打标 Prompt 作为 Few-shot 示例
- 产物：`golden_examples.jsonl`（30 条，含标注结果 + 推断理由）

**任务 3：提出画像假设**

在 AI 聚类之前，先用研专家凭经验提出预期画像类型，作为后期校验的参照。

- 基于对射击品类的理解和笔录的阅读，提出 5-8 个典型玩家画像假设
- 每个画像描述：
  - 画像名称（如"竞技驱动·高能力·PC 端·独狼型"）
  - 一句话描述（这个画像的玩家是什么样的）
  - 关键区分特征：在冰山五层和框架四维度上的典型取值
  - 预期该画像在测试题集中的表现（如"这类玩家在游戏立项测试中会对硬核拟真方向更感兴趣，对付费便捷度不太敏感"）
- 这组假设作为后期验证 AI 聚类画像的参照——AI 产出的画像是否和专家预期一致？不一致的地方是 AI 发现了新 pattern 还是聚类偏了？
- 产物：`画像假设.md`

**任务 4：审核 AI 打标质量**

AI 打标不是一次就对的，需要人工抽检校准。

- 从 AI 打标结果中抽检 30 条（与黄金案例不重复），逐条审核
- 逐条打分：正确（五层都合理）、部分正确（1-2 层有偏差）、错误（整体偏移）
- 统计各维度的准确率：哪层标得最准（通常是 M5 行为和 M4 感受）、哪层最差（通常是 M1 动机和 M2 期待——因为需要更深推断）
- 汇总错误模式（如"LLM 倾向于把'M3 认知'抬高到'M2 期待'"），反馈给 AI 开发修正打标 Prompt
- 如果准确率 < 70%，需要 AI 开发重调 Prompt 后重新打标、重新抽检
- 产物：`打标审核报告.md`

**任务 5：校验聚类画像**

AI 聚类产出的画像是否合理？用研专家来做最终判断。

- 逐个审核 AI 产出的每个画像：
  - 这个画像是否对应真实存在的一类玩家？（可以对照笔录中的具体用户验证）
  - 画像的标签组合是否内部自洽？（如"竞技驱动+高能力+PC端+独狼"是自洽的，"竞技驱动+纯PVE"则不自洽）
  - 画像之间的区分是否清晰？（两个画像的标签太接近说明聚类粒度太细，应该合并）
  - 是否有明显遗漏的玩家类型？（对照画像假设，看 AI 是否漏掉了某类重要用户）
- 提出调整建议：合并某两个画像/拆分某个画像/新增某个画像类型
- 产物：`画像校验报告.md`

---



## MVP 功能清单（P0）


| 功能    | 说明                                   |
| ----- | ------------------------------------ |
| 标签选择器 | 五维度选标签，实时反馈匹配画像数，互斥规则校验              |
| 画像预览  | 选中标签后展示匹配的画像列表，点击查看详情（标签+动机链+原声）     |
| 虚拟访谈室 | SSE 流式对话，气泡布局，打字机效果                  |
| 对话引擎  | 角色 System Prompt + RAG 检索 + LLM 流式生成 |
| 证据溯源  | 点击 AI 回答查看引用的原文片段 + 冰山标签路径           |


暂缓：KOL 分身、多模拟用户同时对话、对话导出、管理后台、登录认证

---



## 关键约定



### 标注数据格式

```json
{
  "source_file": "座谈会笔录-G1.docx",
  "segment_index": 1,
  "original_text": "跟我一起玩的姐妹都不玩了...",
  "annotation": {
    "M5_behavior":    { "value": "降低游玩频率，转向派对游戏", "evidence": "干脆去玩派对游戏了", "confidence": 0.9 },
    "M4_feeling":     { "value": "烦躁、委屈", "evidence": "赢了说我躺，输了说我菜", "confidence": 0.85 },
    "M3_perception":  { "value": "独自玩=负面体验，陌生玩家会评判我", "evidence": "一个人打路人局体验特别差", "confidence": 0.8 },
    "M2_expectation": { "value": "队友应配合而非评判", "evidence": "赢了说我躺，输了说我菜", "confidence": 0.7 },
    "M1_motivation":  { "value": "归属感 + 逃避/放松", "evidence": "至少和朋友一起开心", "confidence": 0.65 },
    "framework": {
      "诉求": ["社交互动", "放松解压"],
      "资产": ["情绪资产稀缺"],
      "能力": "中等",
      "风格": ["社交体验 > 单人体验", "熟人开黑 > 陌生人组队"],
      "甜区": ["团队协作"]
    }
  }
}
```



### API

```
GET  /api/tags              → { dimensions: [{ name, values[] }] }
GET  /api/personas?tags=    → [{ id, name, description, tagSpec, matchScore }]
GET  /api/personas/:id      → { ...persona, evidenceTexts[] }
POST /api/chat              → SSE stream { content_chunk, evidence_ids[], sessionId }
GET  /api/chat/sessions/:id → { id, personaId, messages[] }
```



### 评测标准

- 一致性测试通过率 ≥ 70%（同画像回答不矛盾，不同画像可区分）
- 假设性测试平均评分 ≥ 3/5（回答符合画像人设和动机链）
- 盲测评委区分准确率 ≤ 50%（AI 与真人难以区分）

---

