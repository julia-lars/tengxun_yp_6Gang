# 朱丽烨 — 开发日志

> 角色：技术负责人 + 用研专家

## 2026-07-23
- **项目初始化**：基于 web-dev-tutorial 脚手架创建 monorepo，配置 Turborepo + Biome + TSConfig
- **计划文档**：编写 README.md，定义角色分工与 7 类任务
- **pgvector 安装**：从源码编译 pgvector 0.8.5 到 PG16，建 HNSW 索引；pglite 兼容处理
- **验证**：确认数据库 + LLM SDK + API 路由 + 前端页面全部实现，17 个测试全绿

## 2026-07-28
- KOL 种子数据导入：解析两位 UP 主 JSON 文件，清洗 + 入库 389 条片段

## 2026-07-29
- **KOL 全链路开发**：新增 kol_profiles + kol_segments 表，Drizzle migration；KOL API（列表/详情/SSE 对话）；KOL 前端（列表页/详情页/虚拟对话室）；LLM 画像提取（extractWithLLM）+ 套话清洗（cleanText）
- **部署配置修复**：镜像名 web-dev-tutorial → muru-thinktank；生产 compose 替换为 pgvector 镜像；deploy.yml 改用 GHCR + GITHUB_TOKEN
- **线上部署**：腾讯云 CVM（49.232.59.125），Docker 三容器，deploy.sh 一键更新脚本
- **项目计划**：编写 README2.md（KOL 项目计划）

## 2026-07-30
- **Embedding 选型探索**：先后尝试 transformers.js → TF-IDF → HuggingFace 直连 → ModelScope 镜像，最终落地 bge-large-zh-v1.5 + Python FastAPI 微服务
- **Embedding 实现**：scripts/embed_kol.py（批量 bge 向量化 + 广告分类），scripts/embed_server.py（查询侧实时 embedding 服务）
- **RAG 升级**：kol.ts 中 ILIKE → pgvector `<=>` 余弦相似搜索，含广告过滤 + ILIKE fallback
- **旧会话兜底**：chat.ts 修复——画像被删时用通用设定继续对话，不报 404
- **中期汇报**：新增部署章节，更新 KOL 数据量至 320 视频
