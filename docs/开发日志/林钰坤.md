 7.26
 
 schema.ts（apps/api/src/db/schema.ts）
  - 新增 speakerRoleEnum（interviewee/moderator）和对应字段到 sourceSegments
  表：speakerId、speakerRole、precedingQuestion、charCount，加了 speakerIdx 索引
  - 新增 respondents 表：(sourceFile, speakerId) 唯一索引，background 用 jsonb
  应对不同来源背景信息深浅不一的情况
  - 补上了 RespondentRow 类型导出

  数据处理计划（docs/数据处理计划.md）
  - 把旧版"只保留被访者发言"改成：识别全部发言 → 为每条被访者发言提取
  preceding_question → 只落盘被访者行
  - JSON 字段对齐 schema.ts
  - 明确 original_text（写入后不动）和 cleaned_text（清洗后回填，下游用
  COALESCE）的关系
  - respondents 从"JSON 惯例"升级为正式 DB 表的说明，加了跨 sheet
  合并判断原则、不确定组别代号原样保留、无背景信息可跳过等操作细节
  - 去重改为"丢弃"而非"合并"，短片段过滤改为占位符判断，新增人工抽样核对步骤

  xlsx 提取（E:\tcwork\openpyxl_work\）
  - 把"玩家行为&乐趣整理"和"经验认知乐趣对比"两个 sheet 拆分完毕：
    - respondents_用户记录表.json：13 条（P1-P13，两个 sheet
  合并，有内容交叉印证；P14/P15 无背景信息跳过）
    - segments_玩家行为乐趣整理.json：365 条
    - segments_经验认知乐趣对比.json：90 条（排除了已转入 respondents
  的"游戏经验情况"行）
  - 拆分脚本留在 split_respondents.py

  - 明确 original_text（写入后不动）和 cleaned_text（清洗后回填，下游用
  COALESCE）的关系
  - respondents 从"JSON 惯例"升级为正式 DB 表的说明，加了跨 sheet
  合并判断原则、不确定组别代号原样保留、无背景信息可跳过等操作细节
  - 去重改为"丢弃"而非"合并"，短片段过滤改为占位符判断，新增人工抽样核对步骤

  xlsx 提取（E:\tcwork\openpyxl_work\）
  - 把"玩家行为&乐趣整理"和"经验认知乐趣对比"两个 sheet 拆分完毕：
    - respondents_用户记录表.json：13 条（P1-P13，两个 sheet
  合并，有内容交叉印证；P14/P15 无背景信息跳过）
    - segments_玩家行为乐趣整理.json：365 条
    - segments_经验认知乐趣对比.json：90 条（排除了已转入 respondents
  的"游戏经验情况"行）
  - 拆分脚本留在 split_respondents.py