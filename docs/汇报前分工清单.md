# 周日汇报前分工清单

> 生成时间：2026-09-15（周二）｜ 汇报时间：2026-09-20（周日）
> 本清单说明：每位成员本周要做什么、仓库里现有什么、去哪里找对应文件。

---

## 〇、先对齐：台上统一口径的数字

以下数字已经过核对，**汇报时请以本表为准**，避免各人发言数字互相矛盾：

| 口径 | 数字 | 出处 |
|---|---|---|
| git 提交 / 贡献者 | 108 个提交、8 位代码贡献者 | `git rev-list --count HEAD` |
| 访谈数据 | 14 项目，18,743 条 → 15,395 条有效片段 | `data/群体画像v2.0_merged/`（14 个 JSON） |
| AI 打标 | v3.0：冰山五层 M1-M5 + 框架四维 + 有效性判断 | `scripts/label_all_v3.py` |
| Embedding | bge-m3，10,897 条片段向量化，HNSW 索引 | `data/embed/segments/` |
| 聚类画像 | 5 个（C1-C5），718 名受访者分配（15% 阈值，支持多标签） | `scripts/classify_respondents.py` |
| KOL 语料 | 320 视频全量采集，128 视频已切分，2,871 条片段 | `data/kol/`（*_segmented.json） |
| C 层评测基线 | 09-01：750 题综合 4.14/5，深度短板 2.93 | `data/eval/reports/群体画像v2.0_综合分析报告_20260901.md` |
| C 层评测复测 | 09-15：610 题，深度 3.83（+0.90）、区分度 4.44（+0.51）、综合 4.25（排除误拒答） | `data/eval/reports/C层评测复测_20260915.md` |
| A 层聚类评测 | 初评 F（分离度问题）；已重跑确认，以 C 层端到端评测作为用户侧质量证据 | `data/eval/reports/cluster_eval_20260915.md` |
| 部署 | 腾讯云 CVM http://49.232.59.125，三容器，线上已更新为 C1-C5 | `deploy/README.md` |

---

## 一、张江宇（测试）

### 本周任务
1. **解读线上复测结果**（周三前出）：线上复测 610 题正在后台执行（周三上午出结果），对比表进汇报材料
2. **KOL 评测执行（可选加分，周四前）**：KOL 测试题集评测还没跑过，跑一轮就是新素材
3. **个体发言素材**：你 5 分钟的"测试体系"部分，用四层评测体系 + 750 题 + 复测闭环讲故事，比讲盲测强

### 现有内容与文件位置
| 内容 | 位置 |
|---|---|
| 四层评测体系设计（A/B/C/D，30+ 指标） | `data/eval/评测体系设计方案_V1.0.md` |
| 指标字典 | `data/eval/评测指标字典_V1.0.md` |
| 题库审查方案（190 题逐题） | `data/eval/题库审查与修改方案_V1.0.md` |
| C 层基线报告（4.14/5） | `data/eval/reports/群体画像v2.0_综合分析报告_20260901.md` |
| 问题修复方案（prompt 优化） | `data/eval/reports/群体画像v2.0_问题修复方案_20260901.md` |
| C 层复测报告（4.25） | `data/eval/reports/C层评测复测_20260915.md` |
| 全量评测明细（610 条含 judge 评语） | `data/eval/results/eval_20260915_104523.json` |
| 评测执行脚本（v3，支持 kol 模式） | `scripts/eval_run_v3.py` |
| 群体画像测试用例（122 题 × 5 画像，重建版） | `data/eval/test_cases_persona_recovered_20260915.json`（本地有，gitignore 覆盖） |
| KOL 测试题样例 | `git show febae24:data/eval/test_cases_kol_sample.json` 可恢复；完整 68 题集在 `KOL数字孪生_测试题集_硬核测评KOL(2).xlsx`（你机器上），用 `scripts/eval_convert.py` 转换 |

---

## 二、林钰坤（AI 开发 / 数据处理）

### 本周任务
1. **补开发日志**（周三截止）：`docs/开发日志/林钰坤.md` 目前停在 7-26，把 8 月的数据流水线工作补上（这是你 5 分钟发言的底稿）
2. **准备数据流水线演示材料**：schema 截图、merged 数据样例、打标样例（冰山五层 + 框架四维）
3. **可选（周日后再做）**：群体画像对话侧 RAG 换成向量检索（embedding 已灌入 10,897 条，改动点在 `README.md` AI 开发任务 7 的 🔧 清单里现成）

### 现有内容与文件位置
| 内容 | 位置 |
|---|---|
| 14 项目 merged 标注数据 | `data/群体画像v2.0_merged/`（14 个 JSON，含 annotation 冰山标注） |
| 受访者 Profile 数据 | `data/群体画像v2.0_profile/` |
| 片段向量（bge-m3 1024 维） | `data/embed/segments/segment_embeddings.json` + `.npy` + `_ids.json` |
| 全流程脚本 | `scripts/process_all.py`（提取→清洗→打标→合并） |
| 打标脚本 | `scripts/label_all_v3.py`、`scripts/label_imur_parallel.py` |
| Embedding / 分类脚本 | `scripts/embed_segments.py`、`scripts/split_embeddings_by_profile.py`、`scripts/classify_respondents.py` |
| 数据库 schema | `apps/api/src/db/schema.ts`（7 张表） |
| Migration SQL | `apps/api/drizzle/` |
| 规范文档 | `docs/数据提取规范.md`、`docs/数据清洗规范.md`、`docs/数据标注规范.md`、`docs/Embedding规范.md`、`docs/用户画像Profile生成规范.md` |
| 你的发言提纲（现成） | `docs/项目汇报-中期.md` 附录「个体发言 1. 林钰坤」 |

---

## 三、闫瑾（交互设计 / AI 开发）

### 本周任务
1. **Figma 高保真稿状态确认**（周三前给结论）：任务表里交互设计还挂着 0/3。要展示就本周补稿，不展示就把讲稿里"Figma 设计稿"的说法删掉——二选一
2. **补开发日志**：`docs/开发日志/闫瑾.md` 停在 7-30，KOL 数据采集（B 站 API → whisper 转写 → 17 万字语料）这段工作补进去
3. **发言素材**：你的"交互设计全面改造"是真实上线的（标签选择器、虚拟访谈室、暗色主题），不用改，直接演示线上页面

### 现有内容与文件位置
| 内容 | 位置 |
|---|---|
| 交互设计相关前端组件 | `apps/web/src/components/`（tag-badge、tri-state-tag、evidence-sheet、confidence-indicator 等） |
| 标签选择器 / 画像页面 | `apps/web/src/routes/personas.tsx`、`apps/web/src/components/personas/` |
| 虚拟访谈室 | `apps/web/src/routes/chat-room.tsx`、`apps/web/src/components/chat/agent-chat.tsx` |
| 视觉规范 | `apps/web/src/styles.css`（MUR 暗色主题 @theme） |
| KOL 数据采集产物 | `data/kol/`（鬼王陆行/冷面叶星星IKGN 的 _all.json 与 _segmented.json） |
| 你的发言提纲（现成） | `docs/项目汇报-中期.md` 附录「个体发言 5. 闫瑾」 |

---

## 四、类恺平（产品经理 / 用研专家）

### 本周任务
1. **画像校验（轻量版，周三前）**：抽查 C 层复测报告里 10 条低分 judge 评语，判断是 AI 问题还是评测问题，产出一页纸结论——这就是"画像准确性校验"的最轻量交付
2. **补验收记录（周四前）**：产品经理任务 3"全程验收把关"一直空着，补一页纸：对照线上功能走查一遍（画像线 + KOL 线），记录通过项与遗留项
3. **发言素材**：用研故事讲"画像假设 v1.1（H1-H8）→ v1.2（5 主画像 C1-C5）重构依据"，比讲标注规范有说服力
4. **补开发日志**：`docs/开发日志/类恺平.md` 目前只有一条 7-27 的记录

### 现有内容与文件位置
| 内容 | 位置 |
|---|---|
| 画像假设 v1.2（§0 是 5 主画像重构结论） | `docs/画像假设.md` |
| 标签体系产品方案 v2.0 | `docs/标签体系产品方案.md` |
| 2.0 标签映射/重新对应方案 | `docs/2.0标签映射优化方案.md`、`docs/2.0标签重新对应方案.md` |
| 用户流程与场景说明 | `docs/用户流程与场景说明.md` |
| 竞品研究报告 | `docs/竞品研究报告.md` |
| 数据标注规范（冰山五层 + 框架四维） | `docs/数据标注规范.md` |
| C 层复测报告（含低分案例清单，抽查对象） | `data/eval/reports/C层评测复测_20260915.md` |
| 评测明细（judge 评语原文） | `data/eval/results/eval_20260915_104523.json` |
| 你的发言提纲（现成） | `docs/项目汇报-中期.md` 附录「个体发言 2. 类恺平」 |

---

## 五、崔续衡（AI 开发 / 产品开发后端）

### 本周任务
1. **补开发日志**（周三截止）：补 LLM SDK、对话引擎、API 路由、画像生成引擎的工作记录
2. **发言素材**：重点讲 LLM SDK 统一封装 + 对话引擎（System Prompt 组装 → SSE 流式 → RAG → 证据溯源），演示时配合线上对话页面
3. **可选（周日后再做）**：`README.md` 里 AI 开发任务 7 的 4 个 🔧 优化项（对话记忆摘要、知识边界验证、System Prompt 配置化）

### 现有内容与文件位置
| 内容 | 位置 |
|---|---|
| LLM SDK | `apps/api/src/lib/llm.ts`（chat/chatStream/embed，TokenHub 网关） |
| 对话引擎 + 画像 Prompt（含深层观点素材） | `apps/api/src/routes/chat.ts`（`buildSystemPrompt`） |
| 通用对话引擎 | `apps/api/src/lib/agent-chat.ts` |
| Boundary Engine（本周刚修复误拒答） | `apps/api/src/lib/boundary-engine.ts`、`normalization.ts`、`ambiguity-judge.ts`、`hard-rules.ts` |
| API 路由 | `apps/api/src/routes/`（personas/chat/kol/import/admin 等） |
| 画像生成引擎 | `scripts/generate_profiles.py`、`scripts/classify_respondents.py` |
| 你的发言提纲（现成） | `docs/项目汇报-中期.md` 附录「个体发言 3. 崔续衡」 |

---

## 六、朱丽烨（技术负责人 / 用研专家）

### 本周任务
1. **全流程演示预演（周四，牵头）**：按讲稿脚本完整走一遍线上：画像列表 → 选标签 → 对话 → 溯源 → KOL 对话 → 雷达图 → 历史对话；同时准备本地 localhost 备用环境
2. ~~决策：线上 KOL 数据口径~~ ✅ **已解决（09-16）**：线上 kol_segments 已更新为 2,871 段语义切分语料（bge-m3 向量 + 广告标注已回填），KOL 对话已验证。演示可直接用
3. **pgweb 处理**（周三前）：讲稿演示速查里的 `http://49.232.59.125:8081` 目前 404——要么 compose 加回 pgweb 容器，要么从演示速查删除
4. **补开发日志** + **技术总结**（汇报后完成也行）

### 现有内容与文件位置
| 内容 | 位置 |
|---|---|
| 部署手册（架构/首次准备/手动部署） | `deploy/README.md` |
| 一键部署脚本（本机用，勿提交：含本机路径） | `deploy.sh`（仓库根目录） |
| 生产 compose | `deploy/docker-compose.prod.yml`（已修 LLM 环境透传） |
| 技术实现方案 / 技术选型记录 | `docs/技术实现方案.md`、`docs/技术选型记录.md` |
| KOL 数据与分段产物 | `data/kol/`（含 `archive_1309seg/`、`.rechunk_checkpoint/`） |
| KOL 重切分 / 重导入脚本 | `scripts/rechunk_kol.py`、`scripts/reimport_kol_segments.py` |
| 部署流水线（CI/CD） | `.github/workflows/deploy.yml`（⚠️ Actions 额度耗尽中，近期用 deploy.sh 手动部署） |
| 你的发言提纲（现成） | `docs/项目汇报-中期.md` 附录「个体发言 6. 朱丽烨」 |

---

## 七、全体事项

| 事项 | 截止 | 说明 |
|---|---|---|
| 补开发日志 | 周三 | 各自 `docs/开发日志/<姓名>.md`，目前全部停在 7 月底 |
| 读一遍汇报文档 | 周四前 | `docs/项目汇报-中期.md`（v1.2 已更新全部数字），发言前请对照自己的段落 |
| 口径自查 | 周四前 | 用本清单第〇节的数字表核对自己的发言内容 |
| 演示预演 | 周四 | 朱丽烨牵头，全员参加，按讲稿脚本走一遍 |
| 彩排 | 周五 | 按 40 分钟时间分配表走场（见汇报文档附录） |
| 汇报当天 | 周日 | 提前 30 分钟预热线上页面；本地 localhost 备份待命 |

## 八、演示速查（当天用）

| 要展示的 | 地址 |
|---|---|
| 群体画像对话 | http://49.232.59.125/personas |
| KOL 分身对话 | http://49.232.59.125/kol |
| GitHub 仓库 | https://github.com/julia-lars/tengxun_yp_6Gang |
| 评测报告（本地打开） | `data/eval/reports/` |
| 数据流水线产物 | `data/群体画像v2.0_merged/` |
