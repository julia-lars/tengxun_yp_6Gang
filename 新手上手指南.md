# 新手上手指南：从零到提交第一个 PR

> 不需要任何经验。跟着下面 5 步走，每一步都是复制粘贴命令。

---

## 第 1 步：装工具（一次性，10 分钟）

打开终端（Mac 按 Cmd+空格，搜索"终端"），依次复制粘贴下面三条命令：

```bash
# 装 Bun（JavaScript 运行环境）
curl -fsSL https://bun.sh/install | bash

# 装 GitHub CLI（用于提交代码）
brew install gh

# 装 Git（如果没装过）
brew install git
```

---

## 第 2 步：把仓库拉到本地（一次性，2 分钟）

```bash
# 登录 GitHub
gh auth login
# → 选 GitHub.com → 选 HTTPS → 选 "Login with a web browser" → 按提示操作

# 克隆仓库
gh repo clone julia-lars/tengxun_yp_6Gang
cd tengxun_yp_6Gang

# 装项目的依赖
bun install
```

---

## 第 3 步：启动数据库和本地服务（每次开始工作前，2 分钟）

你的电脑需要运行 PostgreSQL 数据库。根据电脑情况二选一：

**方式 A：已装 Docker（推荐）**
```bash
bun run db:up      # 启动数据库容器
```

**方式 B：没装 Docker，用 Homebrew 装了 PostgreSQL**
```bash
# 启动 PostgreSQL
brew services start postgresql@16

# 如果是第一次装，还要创建用户和数据库（只需做一次）
/opt/homebrew/opt/postgresql@16/bin/createuser -s dev
/opt/homebrew/opt/postgresql@16/bin/createdb -O dev webtutor
```

然后：
```bash
bun run db:migrate   # 创建数据库表结构
bun run db:seed      # 灌入初始数据
bun run dev          # 启动前后端
```

打开浏览器访问 **http://localhost:5173**，能看到页面就说明一切正常。

验证一切正常：
```bash
bun run typecheck    # 应该全绿，无报错
bun run test         # 应该显示 86 tests passed
```

---

## 第 4 步：认识仓库结构

```
tengxun_yp_6Gang/
├── 新手上手指南.md          ← 你正在读的文件
├── PROJECT_PLAN.md         ← 项目计划 + 每个人做什么
├── DEV_GUIDE.md            ← 代码写在哪 + 怎么跟 Claude 对话
├── README.md               ← 脚手架技术说明
│
├── apps/                   ← 📝 所有代码
│   ├── api/src/
│   │   ├── db/schema.ts    ← 数据库表定义
│   │   ├── routes/         ← API 接口
│   │   ├── lib/            ← AI 引擎模块
│   │   └── app.ts          ← 路由挂载入口
│   └── web/src/
│       ├── routes/         ← 前端页面
│       ├── components/ui/  ← 可复用的 UI 组件
│       ├── lib/api.ts      ← 前端调用后端的接口
│       └── styles.css      ← 全局样式/主题色
│
├── packages/shared/src/    ← 🔗 前后端共享的类型定义
│   └── types.ts            ← Zod Schema（前后端的"合同"）
│
├── docs/                   ← 📄 用研产出：标注规范、画像假设等
├── data/                   ← 📊 数据文件：标注结果等（不会上传 GitHub）
├── scripts/                ← 🔧 独立脚本：数据清洗、AI 打标等
├── deploy/                 ← 🚀 部署配置
│
├── docker-compose.yml      ← 本地数据库（PostgreSQL）
├── biome.json              ← 代码规范（自动格式化）
├── turbo.json              ← 任务编排
└── tsconfig.base.json      ← TypeScript 配置
```

开几个关键文件看一眼，大概知道里面是什么样就行。Claude 比你更熟悉这些文件，你只需要告诉它你的任务。

## 第 5 步：确认自己的任务

打开仓库里的 `PROJECT_PLAN.md`，找到你的名字和角色。阅读你的任务描述。

然后打开 `DEV_GUIDE.md`，找到你那个角色的「跟 Claude 对话」模板。

---

## 第 6 步：开始工作

### 创建你的分支

```bash
git checkout -b feature/你的任务英文名
# 例如：git checkout -b feature/llm-sdk
```

### 跟 Claude 对话写代码

打开 Claude Code（或 Cursor），**复制 DEV_GUIDE.md 里你那个角色的对话模板**，填上你的具体任务，发给 Claude。

Claude 会在正确的文件里写代码。

### 验证代码

每写完一个功能，让 Claude 跑：
```bash
bun run typecheck && bun run test
```

必须全绿。

### 提交代码

代码没问题后，告诉 Claude：

> "帮我把改动提交并推送，然后创建一个 PR 到 main 分支"

或者手动操作：
```bash
git add .
git commit -m "[角色] 做了什么"
git push origin feature/你的分支名
gh pr create --title "[角色] 做了什么" --body "描述你的改动"
```

---

## 总结：每次工作的流程

```
1. cd tengxun_yp_6Gang
2. git pull origin main          ← 拉最新代码
3. 启动数据库 + bun run dev       ← 启动服务（见第 3 步）
4. git checkout -b feature/xxx   ← 开分支
5. 跟 Claude 对话写代码
6. bun run typecheck && bun run test  ← 验证
7. git commit + push + 提 PR     ← 提交
```

---

## 遇到问题怎么办

| 问题 | 解决 |
|------|------|
| `bun: command not found` | 重开一个终端窗口，或运行 `source ~/.zshrc` |
| `bun install` 报错 | 网络问题，运行 `bun config set install.registry https://registry.npmmirror.com` |
| `bun run dev` 端口被占用 | `lsof -ti :5173 | xargs kill -9` |
| 数据库连不上 | 检查 PostgreSQL 是否启动：`brew services list | grep postgresql` |
| `bun run test` 有红字 | 把错误信息贴给 Claude，让它帮你修 |
| 不知道代码写在哪 | 打开 `DEV_GUIDE.md` 看速查表 |
| 不知道怎么做某个任务 | 打开 `PROJECT_PLAN.md` 找到你的任务描述，复制给 Claude |
